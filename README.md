# Минимальный Telegram‑бот на Python

Этот бот отвечает на команду `/start` простой фразой.

## Установка

1. Установите Python 3.10+.
2. В консоли перейдите в папку проекта:

   ```bash
   cd "/Users/oleg/Desktop/Все/Самообучение программированию/Душа Рада/telegram_bot_v2"
   ```

3. (Рекомендуется) Создайте и активируйте виртуальное окружение:

   ```bash
   python -m venv venv
   source venv/bin/activate  # macOS / Linux
   # или
   # venv\Scripts\activate   # Windows
   ```

4. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

## Настройка токена

**Важно:** токен бота — это секрет. Никому его не передавайте и не выкладывайте в открытый доступ.

1. В Telegram найдите `@BotFather`, создайте или используйте уже созданного бота и получите токен.
2. Установите переменную окружения `TELEGRAM_BOT_TOKEN`:

   ```bash
   export TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН"  # macOS / Linux
   ```

   На Windows (PowerShell):

   ```powershell
   setx TELEGRAM_BOT_TOKEN "ВАШ_ТОКЕН"
   ```

   Либо можно (на свой страх и риск) временно изменить файл `bot.py` и прописать токен прямо в коде:

   ```python
   TOKEN = "ВАШ_ТОКЕН"
   ```

## Запуск бота

Находясь в папке проекта (и при активированном виртуальном окружении, если вы его используете), выполните:

```bash
python bot.py
```

Если всё настроено правильно, в консоли появится сообщение от `python-telegram-bot` о запуске.

Далее:

1. Откройте Telegram.
2. Найдите вашего бота по его username.
3. Напишите ему `/start` — бот должен ответить сообщением «Привет! Я минимальный тестовый бот 🙂».

